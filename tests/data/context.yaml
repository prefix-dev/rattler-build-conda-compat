context:
  name: "foo"
  version: "bla"
  name_version: ${{ name }}-${{ version }}

package:
  name: ${{ name }}
  version: ${{ version }}
  func_result: ${{ compiler('c') }}

build:
  string: ${{ blas_variant }}${{ hash }}_${{ name_version }}
