python:
  # part of a zip_keys: python, python_impl, numpy
  - 3.8.* *_cpython
  - 3.9.* *_cpython

python_impl:
  # part of a zip_keys: python, python_impl, numpy
  - cpython
  - cpython
numpy:
  # part of a zip_keys: python, python_impl, numpy
  - 1.22
  - 1.22

liboost:
  # test trailing zeros
  - 0.10.01.001
  - 0.10

ruby:
  - if : unix
    then: 2.5
