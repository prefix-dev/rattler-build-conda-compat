name: rattler-build-conda-compat
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.8
  # Development dependencies
  - pip
  - python-build
  - setuptools>=45
  - rattler-build
  - conda-build
  - pytest
  - syrupy
  - ruff
